# Gitton 🐱

A version control system for Ableton Live 12.

## Usage

### Desktop Client

The desktop client has the [Wails framework](https://wails.io/) as its primary development dependency.

To run the development mode, navigate to `/app` and run `wails dev`.

### CLI

In `/cli`, simply run `make` to build the binary to `/cli/bin`.
